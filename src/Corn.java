public class Corn extends Crop{
    public Corn(int growthStage, String nutrientRequirements, String susceptibilityToDiseases, double cropWaterRequirements) {
        super("Corn", growthStage, nutrientRequirements, susceptibilityToDiseases, cropWaterRequirements);
    }
}
